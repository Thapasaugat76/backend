<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Register</title>
  <link rel="stylesheet" href="/style.css">
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: #666363; /* White background */
      color: #000000; /* Black text */
      display: flex;
      flex-direction: column;
      align-items: center;
      padding-top: 60px;
      height: 100vh;
    }

    .head {
      font-size: 2.5rem;
      margin-bottom: 20px;
      text-shadow: 0px 4px 10px rgba(0, 0, 0, 0.4); /* Light shadow for contrast */
    }

    form {
      background: rgba(255, 255, 255, 0.9); /* Solid white with slight transparency */
      padding: 30px;
      border-radius: 15px;
      box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1); /* Subtle shadow */
      width: 90%;
      max-width: 400px;
      display: flex;
      flex-direction: column;
      gap: 15px;
    }

    input, textarea {
      padding: 12px;
      background: #f5f5f5; /* Light gray background for inputs */
      border: 1px solid #ddd; /* Light border */
      border-radius: 8px;
      color: #000000; /* Black text */
      font-size: 1rem;
    }

    input:focus {
      outline: none;
      border-color: #000000; /* Black border on focus */
      background-color: #ffffff; /* White on focus */
    }

    button {
      padding: 12px;
      background-color: #000000; /* Black button */
      border: none;
      border-radius: 8px;
      color: #ffffff; /* White text */
      font-size: 1rem;
      cursor: pointer;
      transition: 0.3s;
    }

    button:hover {
      background-color: #333333; /* Dark gray on hover */
    }

    a {
      color: #000000; /* Black links */
      text-decoration: none;
    }

    .home-button {
      background-color: transparent;
      border: 1px solid #000000; /* Black border */
      color: #000000; /* Black text */
      transition: 0.3s;
    }

    .home-button:hover {
      background-color: #000000; /* Black background on hover */
      color: #ffffff; /* White text on hover */
    }

    .footer {
      margin-top: 20px;
      font-size: 0.9rem;
      text-align: center;
      opacity: 0.7;
    }
  </style>
</head>
<body>

  <h2 class="head">Register</h2>
  <form action="/register" method="POST">
    <input type="text" name="Username" placeholder="Username" required>
    <input type="password" name="Password" placeholder="Password" required>

    <button type="submit">Register</button>
    <button type="button" class="home-button"><a href="/">Home</a></button>
    <footer>Already have an account? <button type="button" class="home-button"><a href="/signup">Login</a></button></footer>

    <div class="footer">
      Contact Us: KAZUWA@gmail.com
    </div>
  </form>

</body>
</html>
